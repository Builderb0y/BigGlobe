{
	"type": "bigglobe:script",
	"biomes": "#bigglobe:has_structure/small_house",
	"spawn_overrides": {},
	"step": "surface_structures",
	"layout": [
		"int originY = world_traits.`bigglobe:y_level_on_surface`",
		"WoodPalette palette = `bigglobe:overworld/wood_palette` ?: return()",

		"pieces.add(",
			"ScriptStructurePiece.new(",
				"originX - 4, originY, originZ - 4, originX + 4, originY + 7, originZ + 4,",
				"'bigglobe:small_house',",
				"nbtCompound(",
					"palette: palette.id",
				")",
			")",
			".symmetrify(Symmetry.randomRotation())",
		")",

		"random.if (",
			"double angle = random.nextDouble(tau)",
			"double radius = random.nextDouble(12.0L, 20.0L)",
			"int*(",
				"farmX = roundInt(fastCos(angle) * radius + originX)",
				"farmZ = roundInt(fastSin(angle) * radius + originZ)",
				"sizeX = random.nextInt(3, 6)",
				"sizeZ = random.nextInt(3, 6)",
			")",
			"pieces.add(",
				"ScriptStructurePiece.new(",
					"farmX - sizeX, originY - 1, farmZ - sizeZ, farmX + sizeX, originY + 1, farmZ + sizeZ,",
					"'bigglobe:farm',",
					"nbtCompound(palette: palette.id)",
				")",
				".symmetrify(random.if ('ROTATE_90') else ('IDENTITY'))",
			")",
		")"
	]
}