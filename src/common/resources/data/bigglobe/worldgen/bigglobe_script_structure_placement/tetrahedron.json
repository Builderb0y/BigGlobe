{
	"raw_placement": [
		"int*(",
			"rawScale = data.scale.asInt()",
			"iterMinX = max(minX, chunkMinX)",
			"iterMinY = max(minY, chunkMinY)",
			"iterMinZ = max(minZ, chunkMinZ)",
			"iterMaxX = min(maxX, chunkMaxX)",
			"iterMaxY = min(maxY, chunkMaxY)",
			"iterMaxZ = min(maxZ, chunkMaxZ)",
		")",
		"BlockState state = data.state.asString().as(BlockState)",
		"for positionLoop (",
			"int y in range[iterMinY, iterMaxY],",
			"int z in range[iterMinZ, iterMaxZ],",
			"int x in range[iterMinX, iterMaxX]",
		":",
			"int*(",
				"scale = 1 << rawScale",
				"iterX = x - midX",
				"iterY = y - midY",
				"iterZ = z - midZ",
				"history = 2x01101001",
			")",
			"repeat (rawScale + 1:",
				"scale >>= 1",
				"int flag = 0",
				"if (iterX >= 0: flag |= 1 iterX -= scale) else (iterX += scale)",
				"if (iterY >= 0: flag |= 2 iterY -= scale) else (iterY += scale)",
				"if (iterZ >= 0: flag |= 4 iterZ -= scale) else (iterZ += scale)",
				"if (history & (1 << (15 - flag)) != 0: break())",
				"if (history & (1 <<       flag ) == 0: continue(positionLoop))",
				"history |= 2x1_0000_0001 << flag",
			")",
			"setBlockState(x, y, z, state)",
		")"
	],
	"placement": "noop"
}