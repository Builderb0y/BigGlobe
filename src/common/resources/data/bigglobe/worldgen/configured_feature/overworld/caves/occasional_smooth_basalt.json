{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"random.nextBoolean(0.25L).if (",
				"int y = 0",
				"while (getBlockState(0, :++y, 0) ==. 'minecraft:basalt[axis=y]':",
					"setBlockState(0, y, 0, 'minecraft:smooth_basalt')",
				")",
			")"
		]
	}
}