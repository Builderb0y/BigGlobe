{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"random.if (0.125:",
				"int x = originX + random.nextInt(-2, 3)",
				"int y = originY + 1",
				"int z = originZ + random.nextInt(-2, 3)",
				"if (getBlockState(x, y, z).isAir():",
					"setBlockState(x, y, z, BlockState('minecraft:skeleton_skull', rotation: random.nextInt(16)))",
				")",
				"return(true)",
			")",
			"else (",
				"return(false)",
			")"
		]
	}
}