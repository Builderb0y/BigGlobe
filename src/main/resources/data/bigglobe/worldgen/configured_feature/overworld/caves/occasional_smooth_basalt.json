{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"random.nextBoolean(0.25L).if (",
				"while (getBlockState(originX, :++originY, originZ) == BlockState('minecraft:basalt[axis=y]'):",
					"setBlockState(originX, originY, originZ, 'minecraft:smooth_basalt')",
				")",
			")"
		]
	}
}