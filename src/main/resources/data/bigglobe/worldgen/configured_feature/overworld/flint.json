{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"random.if (1.0L / 1024.0L:",
				"if (getBlockState(originX, originY, originZ).isReplaceable():",
					"while (getBlockState(originX, originY - 1, originZ).isReplaceable():",
						"--originY",
						"unless (isYLevelValid(originY): return(false))",
					")",
					"placeBlockState(originX, originY, originZ, 'bigglobe:flint[waterlogged=false]')",
					"return(true)",
				")",
			")",
			"return(false)"
		]
	}
}