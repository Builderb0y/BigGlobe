{
	"type": "bigglobe:overworld_structure_overrider",
	"config": {
		"script": [
			"if (start.structure.type == StructureType('bigglobe:mega_tree'):",
				"StructurePiece first = start.pieces.(0)",
				"return(`overworld/foliage`(first.midX, first.midZ) > 0.0L)",
			")",
			"else (",
				"return(true)",
			")"
		]
	}
}