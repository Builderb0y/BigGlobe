{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"return(",
				"random.nextBoolean(1.0 / 96.0) &&",
				"getBlockState(originX, originY + 1, originZ).isAir() &&",
				"placeFeature(originX, originY, originZ, 'bigglobe:overworld/waterfall')",
			")"
		]
	}
}