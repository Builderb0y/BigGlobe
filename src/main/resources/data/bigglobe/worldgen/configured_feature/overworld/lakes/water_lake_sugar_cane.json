{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"return(",
				"getBlockState(originX, originY - 1, originZ).getBlock() == block('minecraft:mud') &&",
				"random.nextBoolean(0.0625) &&",
				"placeFeature(originX, originY, originZ, 'bigglobe:overworld/sugar_cane')",
			")"
		]
	}
}