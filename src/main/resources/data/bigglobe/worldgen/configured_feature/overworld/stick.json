{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"random.if (`overworld/surface_foliage` / 16.0L:",
				"if (originY >= int(`overworld/sea_level`) && getBlockState(originX, originY, originZ).isReplaceable():",
					"while (getBlockState(originX, originY - 1, originZ).isReplaceable():",
						"--originY",
						"if (originY < int(`overworld/sea_level`): return(false))",
					")",
					"placeBlockState(originX, originY, originZ, 'bigglobe:stick[waterlogged=false]')",
					"return(true)",
				")",
			")",
			"return(false)"
		]
	}
}