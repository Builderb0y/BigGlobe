{
	"type": "bigglobe:script",
	"config": {
		"script": [
			"(random.nextBoolean(`overworld/hilliness` / 64.0L) && getBlockState(originX, originY, originZ).isReplaceable()).if (",
				"block (",
					"unless (isYLevelValid(originY): return(false))",
					"else if (getBlockState(originX, originY - 1, originZ).isReplaceable(): --originY,, continue())",
				")",
				"placeBlockState(originX, originY, originZ, 'bigglobe:rock[waterlogged=false]')",
			")"
		]
	}
}